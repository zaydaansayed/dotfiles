##############################################
########## HYPRIDLE AWAKE CONFIG #############
##############################################

# Awake mode: hypridle runs but has NO listeners,
# so the screen never auto-dims and never auto-locks.
# Managed by hypridle-toggle.sh (SUPER+I) — do not edit by hand.

general {
    lock_cmd = pidof hyprlock || hyprlock
}
