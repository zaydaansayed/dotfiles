;; =============================================================================
;; popups.yuck
;; =============================================================================

; Volume popup window
(defwindow volume
  :monitor 0
  :geometry (geometry :x "0px" :y "3px" :width "192px" :height "18px" :anchor "top center")
  :stacking "fg" 
    (box :orientation "h"
         :space-evenly false
      (progress :orientation "h" 
                :value {volinfo.volume}
               :class "volbar")
        (label :text {volinfo.icon})))

; Brightness popup window
(defwindow brightness
  :monitor 0
  :geometry (geometry :x "0px" :y "3px" :width "192px" :height "18px" :anchor "top center")
  :stacking "fg"
    (box :orientation "h"
         :space-evenly false
      (progress :orientation "h" 
                :value {briginfo.brightness}
                :class "brigbar")
        (label :text {briginfo.icon})))

; Notification popup window
(defwindow notification_popup
  :monitor 0
  :geometry (geometry :x "6px" :y "3px" :width "300px" :height "0px" :anchor "top right")
  :stacking "fg"
  (box :orientation "v"
       :space-evenly false
       :class "window"
    (box :orientation "h"
         :space-evenly false
      (label :text notif_app)
      (button :onclick "eww close notification_popup && makoctl dismiss"
               :halign "end"
               :hexpand true
         (label :text "")))
    (label :show-truncated false
           :wrap true
           :text notif_title
           :halign "start")
    (label :show-truncated false
           :wrap true
           :text notif_body
           :xalign 0)))

; Most other files are found in the variables.yuck file, but the processes that calls the windows are in the hyprland.lua file

; For more info go to https://github.com/zaydaansayed/dotfiles/blob/main/SUPPORT.md

;; =============================================================================
;; Made by Zaydaan Sayed 2026
;; =============================================================================
